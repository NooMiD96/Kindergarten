import * as React from "react";

type TProps = {

}

type TState = {

}

export default class Medicament extends React.PureComponent<TProps, TState> {
  state: TState = {
  };

  render() {
    return (
      <React.Fragment>
        Отчёт посещаемости
      </React.Fragment>
    );
  }
}
